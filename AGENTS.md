# Project Guidelines & Rules

## 前端 UI 开发规范 (Mandatory Frontend Design Rule)

在本项目中修改、重构或新增任何前端代码（页面、组件、样式、CSS 变量、布局）时，**必须默认将根目录下的 [DESIGN.md](./DESIGN.md) 作为唯一视觉与 UI 规范依据**：

1. **配色与暗黑主题**：使用 Midnight Base (`#0B0F14`)、Dark Surface (`#141922`)、Elevated Surface (`#1C2330`)、Digital Lavender (`#C8B6FF`) 与 System Green (`#B8E6D0`)。不得随意引入未在 `DESIGN.md` 中定义的第三配色。
2. **字体与 Fluid Clamp 缩放**：标题与 Display 使用 `Anton` / `Inter`，正文使用 `Inter`，标签与数据使用 `IBM Plex Mono` / `JetBrains Mono`。所有 Heading 与 Body 字号必须采用 `clamp()` 流式字号响应规则。
3. **圆角与间距系统**：小型元素 `4px`、标准组件 `12px`、大型卡片/容器 `16px`、胶囊 `9999px`，遵循 8px 网格基准。
4. **组件风格**：卡片采用 `1px solid rgba(255,255,255,0.12)` 边框与演示文稿 Deck Slide 样式，悬停使用 `transform: translateY(-4px)` 及紫色辉光边框过渡。
5. **案例模块架构规范**：所有案例详情页统一放置于 `src/pages/cases/<CaseName>/` 目录下，保持独立自包含结构（独立 JSX 与 CSS 模块），由 `ProjectDetail/index.jsx` 统一进行路由分发。

## 待办同步规则 (Mandatory Todo Sync Rule)

在本项目中修改代码、页面、组件、样式或设计方案前，必须先查看待办：

1. **待办入口**：
   - `docs/project/content-readiness.md`：项目证据缺口与内容就绪状态。
   - `docs/superpowers/plans/`：实现计划中的未完成任务，优先检查当前计划的复选框状态。
   - `docs/site/content-map.md`：案例准入、候选项目与页面映射状态。
2. **改前检查**：如果本次改动与任一待办相关，必须处理该待办，或在改动中明确说明暂不处理及原因；不得以“与视觉无关”等理由跳过。
3. **改后同步**：完成改动后必须同步更新待办文档——完成项标记为已完成（勾选/状态变更，注明日期或来源），新增缺口或任务写入所属层文档（事实缺口 → `content-readiness.md`，实现任务 → `plans/`，站点状态 → `content-map.md`）。
4. **提交一致性**：待办文档更新应与对应代码或设计改动一起提交，除非用户明确要求仅改代码。

## 主动思考与建议规范 (Mandatory Judgment Rule)

在本项目中，无论用户是提问、提出修改还是下达需求，AI 都必须先形成并表达自己的判断，再决定如何回应或执行：

1. **先思考再回应**：结合 `DESIGN.md`、`AGENTS.md`、待办状态与事实库，先说明自己的理解、建议、风险或更优路径，再执行；不把用户原话当作唯一正确方案。
2. **冲突提示**：若需求与现有规范、证据就绪度或待办冲突，必须明确指出冲突和影响，并给出替代方案；除非用户确认，否则不强行执行有冲突的改动。
3. **模糊需求**：需求信息不足时，先说明合理假设与可选方案，再动手；关键决策点用简短建议引导用户确认，不臆造事实。
4. **执行反馈**：执行中发现新风险或更优方案时，应及时更新建议；完成后说明实际判断与用户原要求的差异。
5. **用户最终决定**：当用户明确确认或指示“按原方案执行”时，以用户最终决定为准，但仍保留必要的风险提示。
