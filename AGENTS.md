# Project Guidelines & Rules

## 前端 UI 开发规范 (Mandatory Frontend Design Rule)

在本项目中修改、重构或新增任何前端代码（页面、组件、样式、CSS 变量、布局）时，**必须默认将根目录下的 [DESIGN.md](./DESIGN.md) 作为唯一视觉与 UI 规范依据**：

1. **配色与暗黑主题**：使用 Midnight Base (`#0d0c11`)、Dark Surface (`#16151c`)、Elevated Surface (`#1f1d26`)、Digital Lavender (`#d0bcff`，主交互)、System Mint (`#b9f2c8`，大标题渐变与结果) 与 Soft Pink (`#ffd8e4`，案例标题渐变与装饰)。三种强调色职责固定：Lavender 负责行动、Mint 负责标题与结果、Pink 负责案例标题，不得互换职责，也不得随意引入未在 `DESIGN.md` 中定义的第四配色。
2. **字体与 Fluid Clamp 缩放**：Display 使用 `Anton`（仅拉丁，可 `scaleY(1.05–1.2)` 拉伸与渐变填充），正文使用 `Hanken Grotesk`，衬线叙事层使用 `Merriweather`（300/400），标签与数据使用 `IBM Plex Mono` / `JetBrains Mono`。所有声明的字体必须真实加载（Google Fonts 或自托管），禁止声明未加载的假字体。所有 Heading 与 Body 字号必须采用 `clamp()` 流式字号响应规则。
3. **圆角与间距系统**：小型元素 `4px`、标准组件 `12px`、大型卡片/容器 `16px`、案例卡文件夹顶部 `24px`、胶囊 `9999px`，遵循 8px 网格基准；组件嵌套时内层圆角必须小于外层。
4. **组件风格**：卡片采用文件夹式描边（顶部圆角 `24px`，用 masked `::before` 绘制 1px 边框向下渐隐），悬停使用 `transform: translateY(-2px)` 与 `rgba(208,188,255,0.34)` 边框过渡，不做大范围紫色辉光。
5. **案例模块架构规范**：所有案例详情页统一放置于 `src/pages/cases/<CaseName>/` 目录下，保持独立自包含结构（独立 JSX 与 CSS 模块），由 `ProjectDetail/index.jsx` 统一进行路由分发。

## 待办同步规则 (Mandatory Todo Sync Rule)

在本项目中修改代码、页面、组件、样式或设计方案前，必须先查看待办：

1. **待办入口**：
   - `docs/project/content-readiness.md`：项目证据缺口与内容就绪状态。
   - `docs/superpowers/plans/`：实现计划中的未完成任务，优先检查当前计划的复选框状态。
   - `docs/site/content-map.md`：案例准入、候选项目与页面映射状态。
2. **改前检查**：如果本次改动与任一待办相关，必须处理该待办，或在改动中明确说明暂不处理及原因；不得以“与视觉无关”等理由跳过。
3. **改后同步**：完成改动后必须同步更新待办文档——完成项标记为已完成（勾选/状态变更，注明日期或来源），新增缺口或任务写入所属层文档（事实缺口 → `content-readiness.md`，实现任务 → `plans/`，站点状态 → `content-map.md`）。
4. **提交一致性**：待办文档更新应与对应代码或设计改动一起提交，除非用户明确要求仅改代码。

## 专业伙伴与主动建议规范 (Mandatory Partner & Judgment Rule)

在本项目中，无论用户是提问、提出修改、调整方案还是下达需求，AI 都必须定位为专业的产品/技术对齐伙伴，严禁盲目机械执行：

1. **先思考评估，后提供参考**：收到修改建议或新需求时，先结合 `DESIGN.md`、`AGENTS.md`、代码架构与待办状态进行独立分析，优先输出评估意见：
   - **意图理解与优点**：阐述对用户修改目标的理解及其合理性。
   - **潜在风险与隐患**：明确指出可能引发的 UI 破坏、不符合视觉规范、测试回归风险或逻辑冲突。
   - **专业替代/优化方案**：提供更符合整体设计规范、体验更佳或风险更低的备选路径。
2. **拒绝盲目执行**：不把用户原话当成唯一无条件执行的指令。在给出风险评估和建议前，不直接盲目动手修改代码；引导用户参考分析后再决定如何推进。
3. **冲突与偏差预警**：若用户的修改建议与 `DESIGN.md` 规范、证据就绪度或既有架构冲突，必须明确指出冲突后果，并给出合规的调优建议。
4. **模糊需求主动收敛**：修改意见信息不足或存在歧义时，主动梳理合理假设与可选方案，列出优劣势供用户挑选，不凭空猜测。
5. **尊重大致权衡与最终决定**：当用户在了解风险和建议后，明确确认或指示“按原方案执行”或选择某备选路径时，AI 应以用户的最终决定为准高效执行。

## 项目资产动态发现与查全规范 (Dynamic Project Scope Rule)

在处理“所有项目 / 现有项目 / 全量案例 / 各个项目”相关需求或制定实现计划时，必须严格执行动态发现协议：

1. **解耦展示层与资产库**：严禁以前端当前渲染的卡片数量（如首页 3 卡）作为项目全集假设。
2. **轻量动态扫描（Metadata-Only）**：必须先执行轻量目录扫描（`list_dir` 获取 `docs/project/*.md` 与 `docs/cases/*.md` 文件名，消耗 <100 tokens），以实际存在的案例文件集合作为资产全集。
3. **计划首部显式比对**：在制定涉及多项目的 Plan 时，必须在 Plan 开头明确列出本次动态检测到的完整项目清单（如当前 4 个项目及未来扩展项目），供用户核对确认后再执行。
4. **按需深度加载（Lazy-Loading）**：确认项目范围时仅读取文件名/表格元数据；进入具体编辑时才按需读取单篇正文，杜绝大 Token 浪费。
