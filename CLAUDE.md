# Claude Code Guidelines

## 前端开发默认设计规范

在本项目中处理任何前端 UI、样式、组件开发任务时：

- **强制读取并遵守**: 根目录的 [`DESIGN.md`](./DESIGN.md)
- **配色**: 背景 `#0B0F14` / 卡片 `#141922` / 浮层 `#1C2330` / 主 Accent `#C8B6FF` / 高亮 `#B8E6D0`
- **字体**: 标题 `Anton` / `Inter` / 正文 `Inter` / 标签 `IBM Plex Mono`
- **圆角**: 小标签 `4px` / 按钮 `12px` / 卡片 `16px` / 胶囊 `9999px`
- **响应式**: 使用 CSS `clamp()` 流式字号与 8px 间距网格

