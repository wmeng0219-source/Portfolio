# Claude Code Guidelines

## 前端开发默认设计规范

在本项目中处理任何前端 UI、样式、组件开发任务时：

- **强制读取并遵守**: 根目录的 [`DESIGN.md`](file:///Users/wen/Desktop/Portfolio/DESIGN.md)
- **配色**: 背景 `#0d0c11` / 卡片 `#16151c` / 主 Accent `#d0bcff` / 高亮 `#b9f2c8`
- **字体**: 标题 `Neudron` / 正文 `GT America` / 标签 `IBM Plex Mono`
- **响应式**: 使用 CSS `clamp()` 流式字号与 8px 间距网格
